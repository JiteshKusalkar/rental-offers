import styled from 'styled-components';

const Wrapper = styled.div`
  padding: 30px 0;
  margin: 0 12px;
  border-top: 1px solid #e1dada;
`;
const Section = styled.div``;
const Label = styled.span`
  font-weight: 600;
  margin-right: 5px;
`;
const Value = styled.span``;

export { Label, Section, Value, Wrapper };
